server:
    port: 8081
spring:
    application:
        name: config-service
    cloud:
        config:
            server:
                #configurando el repositorio de git e indicando la rama
                git:
                    default-label: circuit-breaker
                    uri: https://github.com/jcllt10/repositoriomean
                    search-paths: config-data