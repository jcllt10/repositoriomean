server:
    port: 8081
spring:
    application:
        name: config-service
    cloud:
        config:
            server:
                git:
                    default-label: config-server
                    uri: https://github.com/jcllt10/repositoriomean
                    search-paths: config-data