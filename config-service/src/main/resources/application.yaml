server:
    port: 8081
spring:
    application:
        name: config-service
    cloud:
        config:
            server:
                #configurando el repositorio de git cambiando de rama
                git:
                    default-label: gateway
                    uri: https://github.com/jcllt10/repositoriomean
                    search-paths: config-data