spring:
    application:
        name: user-service
    cloud:
        #apuntando al repositorio de config para la configuracion
        config:
            enabled: true
            uri: http://localhost:8081
